#include <stdio.h>
func_prime (int x)
{
    while (x=2;x<=100);
    {
        if (x%2!=0)
        {
          x++;
        }

    return x;
    }
}
int main ()
{
    int i = func_prime (2);
    printf ("%d\n", i);
    return 0;
}
